# Bayou-Server

The bayou server is an easily configurable activitypub server that currently uses postgres for its database driver. Support for multiple dbms, specifically for sqlite is something this project hopes to accomplish down the road for even simpler deployment however it is not a priority and should not be expected anytime soon.

